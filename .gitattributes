# 强制 git 钩子脚本使用 LF 换行
# （Windows 下 git 若把 hook 转成 CRLF，sh 解释器会崩溃，导致提交检查全部失效）
.githooks/* text eol=lf
# .md 文档同样强制 LF，统一行尾并消除 "LF will be replaced by CRLF" 警告
*.md text eol=lf
# .gitattributes 自身也强制 LF（防止 git 把它转成 CRLF 后规则失效）
.gitattributes text eol=lf
